generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"

}

model Spectacle {
  id Int @id @default(autoincrement())
  description String?
  type String
  statut String
  troupe String
}

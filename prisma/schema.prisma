generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Contact {
  id    Int     @id @default(autoincrement())
  nom String 
  prenom String
  date_creation DateTime @default(now())
}

